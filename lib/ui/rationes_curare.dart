import 'package:flutter/material.dart';

class RationesCurare extends StatelessWidget {
  const RationesCurare({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Rationes Curare',
      home: Scaffold(
        appBar: AppBar(
          title: const Text('Rationes Curare'),
        ),
        body: const Center(
          child: Column(
            children: [],
          ),
        ),
      ),
    );
  }
}
